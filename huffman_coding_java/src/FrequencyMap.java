import java.util.LinkedHashMap;
import java.util.Map;

/**
 * Inner class representing the frequency map that we build the tree
 * from.
 */
public class FrequencyMap extends LinkedHashMap<Character, Integer> {
}
